apiVersion: v1
kind: Service
metadata:
  name: devsecops-service
  namespace: devsecops-app
spec:
  selector:
    app: devsecops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
